#include "SalidaDeAudio.h"

SalidaDeAudio::SalidaDeAudio(string nom, double pre, string mod, string car): Componente(nom, pre), modelo(mod), caracteristica(car)
{
}

SalidaDeAudio::~SalidaDeAudio()
{
}
