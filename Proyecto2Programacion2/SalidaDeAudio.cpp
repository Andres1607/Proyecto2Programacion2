#include "SalidaDeAudio.h"

SalidaDeAudio::SalidaDeAudio(string nom, double pre, string mod, string car): nombre(nom), precio(pre), modelo(mod), caracteristica(car)
{
}

SalidaDeAudio::~SalidaDeAudio()
{
}
